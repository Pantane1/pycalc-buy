import { ResourceConfig, PaymentLink, CreatePaymentLinkParams, UpdatePaymentLinkParams, ListPaymentLinksParams } from '../types';
export declare class PaymentLinks {
    private http;
    constructor(config: ResourceConfig);
    /**
     * List all payment links
     */
    list(params?: ListPaymentLinksParams): Promise<PaymentLink[]>;
    /**
     * Retrieve a specific payment link
     */
    retrieve(id: string): Promise<PaymentLink>;
    /**
     * Create a new payment link
     */
    create(params: CreatePaymentLinkParams): Promise<PaymentLink>;
    /**
     * Update a payment link
     */
    update(id: string, params: UpdatePaymentLinkParams): Promise<PaymentLink>;
    /**
     * Delete a payment link
     */
    delete(id: string): Promise<void>;
    /**
     * Get public payment link (no auth required)
     */
    getPublic(slug: string): Promise<PaymentLink>;
    /**
     * Initiate payment on a payment link
     */
    initiatePayment(slug: string, phone: string, amount?: number): Promise<any>;
}
//# sourceMappingURL=PaymentLinks.d.ts.map