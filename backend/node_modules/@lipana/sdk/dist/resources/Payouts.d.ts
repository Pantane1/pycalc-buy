import { ResourceConfig, Payout, ListPayoutsParams, RequestPayoutParams, TopUpParams, TopUpResponse } from '../types';
export declare class Payouts {
    private apiKey;
    private baseUrl;
    private environment;
    private client;
    constructor(config: ResourceConfig);
    /**
     * List payouts
     */
    list(params?: ListPayoutsParams): Promise<{
        data: Payout[];
        pagination: any;
    }>;
    /**
     * Retrieve a payout
     */
    retrieve(id: string): Promise<Payout>;
    /**
     * Request a manual payout (withdraw funds)
     */
    request(params: RequestPayoutParams): Promise<Payout>;
    /**
     * Send money to a specific recipient
     */
    sendToRecipient(recipientId: string, amount: number): Promise<Payout>;
    /**
     * Send money to all active recipients
     */
    sendToAllRecipients(amount: number): Promise<{
        total: number;
        successful: number;
        failed: number;
        results: any[];
        errors: any[];
    }>;
    /**
     * Top up account via MPESA STK Push
     */
    topUp(params: TopUpParams): Promise<TopUpResponse>;
}
//# sourceMappingURL=Payouts.d.ts.map